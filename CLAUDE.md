# CLAUDE.md — nexus-sphere

## Project Overview
AI/Python research project. `uv` package manager.

## Technology Stack
Python, `uv` for dependency management.

## CI
`uv sync` + pytest.

## AI Agent Rules
- This repo has not had a deep audit in this engagement — verify actual test coverage and maturity directly (see akili-core's CLAUDE.md for an example of a repo where "has test files" turned out not to mean "has real tests") rather than assuming.

## Definition of Done
State plainly whether any test files that exist are real automated tests (pytest with assertions) or manual verification scripts.
